import {createTheme} from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#027353',
        },
        secondary: {
            main: '#F2BD1D',
        },
        }
});

export default theme;
